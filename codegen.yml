overwrite: true
schema: "${NEXT_PUBLIC_API_URL}/graphql"
documents: "service/queries.graphql"
generates:
  service/graphql.tsx:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typed-document-node"
